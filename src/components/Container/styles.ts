import styled from 'styled-components'

export const ContainerComponent = styled.div`
  background: linear-gradient(0deg, #06092B, #06092B), #F1F3FF;
`

export const Wrapper = styled.div`
  max-width: 1256px;
  margin: 0 auto;
  padding-top: 60px;
`